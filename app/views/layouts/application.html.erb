<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="ROBOTS" content="NOODP" />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>
      <% if signed_in? %>
        <%= [content_for(:title), 'Sotwi'].compact.join(' - ') %>
      <% else %>
        Enter
      <% end %>
    </title>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="<%= current_link_name %>">
    <div class="app-container">
      <%= render 'menu' %>

      <%= render 'flashes' %>
      <%= yield %>
    </div>
  </body>
</html>
